{
  "name": "@immunity-wars/perf",
  "version": "0.0.0",
  "private": true,
  "description": "Measurement instruments: P2.3 CDP runs against the dev shell reading __iwMetrics, the Gate 1 audit, and P3.3 frames-or-state (bytes on the wire, through the room).",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "build": "echo \"no build output\""
  },
  "devDependencies": {
    "puppeteer-core": "^25.11.0"
  },
  "dependencies": {
    "@immunity-wars/protocol": "workspace:*",
    "@immunity-wars/room": "workspace:*",
    "@immunity-wars/session": "workspace:*"
  }
}
