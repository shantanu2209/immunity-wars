{
  "name": "@immunity-wars/perf",
  "version": "0.0.0",
  "private": true,
  "description": "P2.3 measurement driver: CPU-throttled CDP runs against the dev shell, reading the page's own __iwMetrics.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "build": "echo \"no build output\""
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
