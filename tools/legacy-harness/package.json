{
  "name": "@immunity-wars/legacy-harness",
  "version": "0.0.0",
  "private": true,
  "description": "Task G: stitch the ported engine into the legacy UI and emit one self-contained HTML file",
  "license": "Apache-2.0",
  "type": "module",
  "comment:read-only-legacy": "This package READS tools/legacy/ and never writes to it. tools/legacy is reference material the project has committed never to edit; the harness substitutes the engine at the /*__ENGINE__*/ marker at BUILD time, into a build output, leaving the source untouched.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "build": "tsx build.ts",
    "frames": "tsx frames.ts",
    "controls": "tsx controls.ts",
    "drift": "tsx drift.ts",
    "seam": "tsx seam.ts",
    "homes": "tsx homes.ts"
  },
  "dependencies": {
    "@immunity-wars/content": "workspace:*",
    "@immunity-wars/engine": "workspace:*",
    "@immunity-wars/equivalence": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  }
}
