{
  "name": "@immunity-wars/session-core",
  "version": "0.0.0",
  "private": true,
  "description": "What every holder of a GameState needs, LocalSession's and the relay's alike: the query builder and the invader-id workaround. P3.4.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "comment:why-this-package-may-import-the-engine": "It computes, from a GameState, what LocalSession and the room both hand out. It is engine-side, like session and room, and may not reach ui, app, server, session or room (session-core-no-downstream).",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@immunity-wars/content": "workspace:*",
    "@immunity-wars/engine": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
