/**
 * The 22 engine queries the UI reads now live in `@immunity-wars/session-core` (P3.4), shared with
 * the relay. Re-exported here unchanged so every existing import keeps resolving.
 */
export {
  ALL_UI_QUERIES,
  CELL_KEYS,
  FAMILIES,
  INVADER_ONLY,
  ORGAN_ONLY,
  PER_CELL,
  PER_FAMILY,
  PER_INVADER,
  PER_ORGAN,
  SCOPED,
  STATE_ONLY,
} from '@immunity-wars/session-core';
