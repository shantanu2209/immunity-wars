# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
*.tsbuildinfo

# Turborepo
.turbo/

# Test / coverage output
coverage/
.nyc_output/
*.lcov

# Environment and secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
secrets.json

# Claude Code — machine-local tool permissions, not project configuration.
# CLAUDE.md at the repo root IS shared and is committed; this file is not.
.claude/settings.local.json

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite
.vite/

# Playwright (Phase 2+)
playwright-report/
test-results/

# Game content documents (the rulebook, quick reference and study packet) ARE committed, since
# 5 September 2026: the content licence is settled — all rights reserved, none declared
# (docs/LICENSES.md) — and a corrected rulebook that exists on one machine is a rulebook waiting
# to be lost. They were ignored while the licence was pending; that reason has expired.

# Coverage output
coverage/

# CI output, written into the working tree by the workflows and by anyone running the same
# commands locally. Ignored rather than merely untracked, on the rule the docx entry above uses:
# untracked is one careless `git add -A` away from being committed, and that has happened here.
#   results/  one JSON per suite, written by tools/ci/record-result.ts
#   site/     the built dashboard, uploaded to Pages and never committed to main
#   history/  the accumulated records, fetched from the orphan results-data branch
results/
site/
history/
